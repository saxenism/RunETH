const baseURL = "http://localhost:5000/api";

const kuchBhi = {
  proofs: `${baseURL}/proofs`,
  testAuthentication: `${baseURL}/testAuthentication`
}

export default kuchBhi;

//axios.get(kuchBhi.proofs